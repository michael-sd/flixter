<br />

<div class="booyah-box col-xs-10 offset-xs-1">
  <h1 class="text-xs-center">Learn Something Today</h1>
  <hr />

  <% @courses.each do |course| %>
    <h2 class="text-xs-center"><%= course.title %></h2>
    <br />
    <div class="col-sm-8 col-xs-12">
      <%= course.description %>
    </div>
    <div class="col-sm-4 col-xs-12 text-xs-center">
      <%= link_to 'Button', '#', class: 'btn btn-primary btn-large' %>
    </div>
    <br class="clr" />
  <% end %>
</div>